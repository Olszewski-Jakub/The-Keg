package com.trackmybus.theKeg.di


import org.koin.dsl.module

val databaseModule = module {
    single {
//        val connection: Connection =
//            getKoin().getProperty("connection") ?: throw IllegalStateException("Connection not found")
//        connection
    }
}